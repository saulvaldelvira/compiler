[package]
name = "ast_tests"
version = "0.1.0"
edition = "2024"

[dependencies]
lexer = { path = "../../lib/lexer" }
parser = { path = "../../lib/parser" }
ast = { path = "../../lib/ast" }
ast_passes = { path = "../../lib/ast_passes" }
