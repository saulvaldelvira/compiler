[package]
name = "hir_identification"
version = "0.1.0"
edition = "2024"

[dependencies]
hir = { path = "../hir" }
interner = { path = "../interner" }
error_manager = { path = "../error_manager" }

[dev-dependencies]
compiler_driver = { path = "../compiler_driver" }

[lints]
workspace = true
