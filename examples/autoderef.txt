mod libc;
use libc::stdio::printf;

fn main() {

    struct S {
        a: i32,
        b: i32,
    }

    let a: S;
    let ptr: &S = &a;
    let doubleptr: &&S = &ptr;


    ptr.a = 12;
    (*doubleptr).b = 12;

    printf("a = %d, b = %d\n", a.a, a.b);
}
